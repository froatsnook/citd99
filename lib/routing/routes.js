Router.map(function() {
    this.route("home", {
        layoutTemplate: "skeleton",
        path: "/"
    });

    this.route("about", {
        layoutTemplate: "skeleton",
        path: "/about"
    });
});

